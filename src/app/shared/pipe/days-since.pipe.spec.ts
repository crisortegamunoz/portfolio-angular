import { DaysSincePipe } from './days-since.pipe';

describe('DaysSincePipe', () => {
  it('create an instance', () => {
    const pipe = new DaysSincePipe();
    expect(pipe).toBeTruthy();
  });
});
